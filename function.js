function addNumbers(a, b) {
    let ans = a + b;
    console.log(`addition is: ${ans}`);
    
}

ans = addNumbers(5, 10); // Calling the function with arguments 5 and 10.  

console.log(ans)

//arrow function
const multiplyNumbers = (x, y) => {
    return x * y;
}

let result = multiplyNumbers(4, 6); // Calling the arrow function with arguments 4 and 6.

console.log(`multiplication is: ${result}`)

